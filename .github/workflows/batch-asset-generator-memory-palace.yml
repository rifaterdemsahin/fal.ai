---
name: Batch Asset Generator - Memory Palace

'on':
  workflow_dispatch:
    inputs:
      commit_and_push:
        description: 'Commit and push generated files'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
  push:
    branches:
      - main
    paths:
      - '5_Symbols/BatchAssetGeneratorMemoryPalace.py'
      - '.github/workflows/batch-asset-generator-memory-palace.yml'

jobs:
  generate-memory-palace:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then python -m pip install -r requirements.txt; fi

      - name: Run BatchAssetGeneratorMemoryPalace
        env:
          FAL_KEY: ${{ secrets.FAL_API_KEY }}
        run: |
          cd 5_Symbols
          python BatchAssetGeneratorMemoryPalace.py

      - name: Display generation summary
        run: |
          SUMMARY_FILE="5_Symbols/generated_memory_palace/generation_summary.json"
          if [ -f "${SUMMARY_FILE}" ]; then
            echo "=== Generation Summary ==="
            cat "${SUMMARY_FILE}"
          fi

      - name: Commit and push generated files
        if: ${{ github.event.inputs.commit_and_push != 'false' }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add 5_Symbols/generated_memory_palace/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Generated memory palace assets via GitHub Actions"
            git push
          fi

      - name: Upload generated assets as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: generated-memory-palace
          path: 5_Symbols/generated_memory_palace/
          retention-days: 30
